"""
tools/ — operator scripts (CLI).

Tools may freely read data/reference/ (the firewall applies only to
engine / sutras / phonology / pipelines).  Tools should NOT modify
engine behaviour; they are pure observers and validators.
"""
